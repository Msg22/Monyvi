# commit-message: chore: remove temporary validation workflows

diff --git a/.github/workflows/codex-apply-patch.yml b/.github/workflows/codex-apply-patch.yml
deleted file mode 100644
index 94cc71d..0000000
--- a/.github/workflows/codex-apply-patch.yml
+++ /dev/null
@@ -1,49 +0,0 @@
-name: Codex Apply Patch
-
-on:
-  push:
-    branches:
-      - codex/103-recurring-payment-validation
-    paths:
-      - .github/codex/changes.patch
-
-permissions:
-  contents: write
-
-concurrency:
-  group: codex-apply-patch-103
-  cancel-in-progress: false
-
-jobs:
-  apply:
-    name: Apply prepared patch
-    runs-on: ubuntu-latest
-    timeout-minutes: 10
-    steps:
-      - name: Checkout repository
-        uses: actions/checkout@v4
-        with:
-          fetch-depth: 0
-          ref: codex/103-recurring-payment-validation
-
-      - name: Apply and commit patch
-        shell: bash
-        run: |
-          set -euo pipefail
-          patch_file=".github/codex/changes.patch"
-          commit_message="$(sed -n '1s/^# commit-message: //p' "$patch_file")"
-          if [[ -z "$commit_message" ]]; then
-            echo "Missing '# commit-message: ...' header" >&2
-            exit 1
-          fi
-
-          tail -n +2 "$patch_file" > /tmp/codex-changes.patch
-          git apply --check /tmp/codex-changes.patch
-          git apply /tmp/codex-changes.patch
-          rm "$patch_file"
-
-          git config user.name "OpenAI Codex"
-          git config user.email "codex@openai.com"
-          git add -A
-          git commit -m "$commit_message"
-          git push origin HEAD:codex/103-recurring-payment-validation
diff --git a/.github/workflows/codex-focused-validation.yml b/.github/workflows/codex-focused-validation.yml
deleted file mode 100644
index 0cc3719..0000000
--- a/.github/workflows/codex-focused-validation.yml
+++ /dev/null
@@ -1,65 +0,0 @@
-name: Codex Focused Validation
-
-on:
-  push:
-    branches:
-      - codex/103-recurring-payment-validation
-    paths:
-      - .github/codex/validation-trigger.txt
-  workflow_dispatch:
-
-permissions:
-  contents: read
-
-concurrency:
-  group: codex-focused-validation-103
-  cancel-in-progress: true
-
-jobs:
-  focused-validation:
-    name: Focused tests and type checks
-    runs-on: ubuntu-latest
-    timeout-minutes: 30
-    env:
-      SENTRYCLI_SKIP_DOWNLOAD: "1"
-    steps:
-      - name: Checkout repository
-        uses: actions/checkout@v4
-        with:
-          fetch-depth: 0
-
-      - name: Setup Node.js
-        uses: actions/setup-node@v4
-        with:
-          node-version-file: .nvmrc
-          cache: npm
-
-      - name: Restore installed dependencies
-        id: dependencies-cache
-        uses: actions/cache@v4
-        with:
-          path: |
-            node_modules
-            apps/*/node_modules
-            packages/*/node_modules
-          key: codex-node-modules-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
-
-      - name: Install dependencies
-        if: steps.dependencies-cache.outputs.cache-hit != 'true'
-        run: npm ci
-        timeout-minutes: 20
-
-      - name: Test shared logic
-        run: npm test -w @monyvi/logic -- --runInBand amount-helpers currency date-boundary
-
-      - name: Test recurring and transaction paths
-        if: always()
-        run: npm test -w @monyvi/mobile -- --runInBand transaction-validation recurring-payment-validation recurring-payment-service create-recurring-payment recurring-payment-header-actions
-
-      - name: Typecheck shared logic
-        if: always()
-        run: npm run typecheck -w @monyvi/logic
-
-      - name: Typecheck mobile
-        if: always()
-        run: npm run typecheck -w @monyvi/mobile
diff --git a/.github/workflows/codex-workspace-export.yml b/.github/workflows/codex-workspace-export.yml
deleted file mode 100644
index 6879f6f..0000000
--- a/.github/workflows/codex-workspace-export.yml
+++ /dev/null
@@ -1,42 +0,0 @@
-name: Codex Workspace Export
-
-on:
-  push:
-    branches:
-      - codex/103-recurring-payment-validation
-  workflow_dispatch:
-
-permissions:
-  contents: read
-
-jobs:
-  export:
-    name: Export repository workspace
-    runs-on: ubuntu-latest
-    timeout-minutes: 10
-    steps:
-      - name: Checkout repository
-        uses: actions/checkout@v4
-
-      - name: Upload workspace
-        uses: actions/upload-artifact@v4
-        with:
-          name: monyvi-workspace
-          path: |
-            .agent
-            .github
-            .specify
-            apps
-            docs
-            packages
-            scripts
-            supabase
-            package.json
-            package-lock.json
-            tsconfig.base.json
-            nx.json
-            eslint.config.*
-            .nvmrc
-            AGENTS.md
-          include-hidden-files: true
-          retention-days: 1
