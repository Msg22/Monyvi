# commit-message: test: preserve the recurring logger mock
--- a/apps/mobile/__tests__/app/recurring-payment-header-actions.test.tsx
+++ b/apps/mobile/__tests__/app/recurring-payment-header-actions.test.tsx
@@ -183,7 +183,9 @@
 
 jest.mock("@/utils/logger", () => ({
   logger: {
-    error: mockLoggerError,
+    error: (...args: readonly unknown[]): void => {
+      mockLoggerError(...args);
+    },
   },
 }));
 
