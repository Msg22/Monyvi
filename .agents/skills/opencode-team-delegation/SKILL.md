---
name: opencode-team-delegation
description:
  "Load the canonical OpenCode worker runtime and usage policy: model
  selection, OpenCode Go limits, usage checks, limit fallback, and session
  discipline. Use only when an OpenCode execution pool is approved. Do not
  auto-trigger."
---

# opencode-team-delegation

Read
[the OpenCode worker delegation workflow](../../../.agent/workflows/opencode-team-delegation.md)
completely, then follow it.

This skill routes OpenCode runtime and usage policy only. It does not replace
`team-led-delivery.md`, `sprint-issue.md`, TDD, review, or handoff workflows;
compose those from the applicable workflow entrypoints.